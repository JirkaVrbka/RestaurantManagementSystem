@model Dictionary<string, int>

@{
    var today = new Chart(width: (Model.Count * 100), height: 400)
        .AddTitle("Items sold overall")
        .AddSeries(
            name: "Items sold overall",
            xValue: Model.Keys,
            yValues: Model.Values)
        .Write();
}